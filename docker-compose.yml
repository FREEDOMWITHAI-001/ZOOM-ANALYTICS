version: '3.8'

services:
  zoom-analytics:
    build: .
    ports:
      - "3005:3000"
    environment:
      - DB_HOST=140.245.206.162
      - DB_PORT=5432
      - DB_NAME=zoomanalytics
      - DB_USER=zoom_admin
      - DB_PASSWORD=ZoomAdmin@2026
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=gpt-4o-mini
    restart: unless-stopped
