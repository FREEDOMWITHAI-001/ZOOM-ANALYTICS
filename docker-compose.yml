services:
  zoom-analytics:
    build: .
    ports:
      - "8020:8020"
    environment:
      - DB_HOST=140.245.206.162
      - DB_PORT=5432
      - DB_NAME=zoomanalytics
      - DB_USER=zoom_admin
      - DB_PASSWORD=ZoomAdmin@2026
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=gpt-4o-mini
      - JWT_SECRET=zm-analytics-jwt-s3cret-k3y-2026-pr0d
    restart: unless-stopped
